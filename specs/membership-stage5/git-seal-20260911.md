# Stage5 PASS Git 封板范围

工作区 D:\WordMasterApp-Membership-Stage2-20260908，独立分支 codex/membership-stage2-20260908，起始基线及封板前 HEAD 为 14129f96d8581968e4fdad8551cc1a47d7a4aa55。Stage2～Stage5 最终验收冻结继承，以本目录 README 和最终 Stage5-E / Stage5-F 记录为准；历史文档中的待办不是新增执行授权。

## 纳入内容

- membership-core 账本、订单、权益及时间模型；membership-payment 平台协议、签名、通知、查单、补偿及令牌适配；membership-access 权限服务。
- membership-stage5 固定范围策略、隔离仓库、运行时、受保护调度和声明式配置；orders / payment_notify / compensate / access / admin 五个入口。
- utils 中支付服务与 Stage5 客户端、app 构造入口、分包注册、学生页验收导航和 Stage5 页面，包括通过服务端恢复跨设备权益及防重复购买。
- scripts/build-stage5-packages.cjs：可复用离线打包工具，固定五个入口和四个共享目录，生成 Node >=18 清单，依赖固定为 @cloudbase/node-sdk 3.16.0、wx-server-sdk 3.0.1；仅打包，不安装或部署。
- Stage2～Stage5 可维护测试及 fixture、设计与安全边界文档、结构化历史验证摘要、最终支付与设备验收记录。
- repairNames 安全源码、专属测试及独立最终记录，不遗漏此前已验证部署的安全补丁。

## 排除与证据边界

原始 .tap、stage5b-*.txt 输出及 TEST 商品图片保留本地，加入精确忽略规则。旧文档对这些日志的引用表示本地历史证据，并不表示文件随 Git 分发。结构化 verification.json 属于长期验收摘要，保留其中记录的历史时间、范围和哈希，不改写成最新状态。

cloud-targets.test.js 是对历史云端源码快照的静态回归，需显式提供 STAGE5_CLOUD_EVIDENCE_DIR；快照不分发，缺失会明确失败。它不能当成无需外部证据的常规测试，也不因本轮封板重新运行。

不纳入截图、备份、原始云端响应、本机工具、缓存、node_modules 或加密凭据容器。文档中受保护证据路径仅用于追溯，不包含其文件内容。project.config.json 的末尾换行差异与功能无关，保留在工作区，不纳入提交。新增凭据容器忽略规则仅防止误暂存，不删除文件。

## 本轮检查与交付约束

仅执行源码语法、JSON 解析、静态相对引用及打包入口清单检查、待提交集合完整性、敏感信息扫描和 git diff --check。真实凭据、完整身份、私钥不允许进入提交；公开配置标识、哈希、明确离线 fixture 和公开协议测试向量不视作真实凭据。

Git 提交及远端 SHA 以本轮交付结果为准；该记录本身不提前声明推送成功。本轮不运行任何支付/权益/云端业务操作，不重跑冻结验收、不部署、不开启正式399商品、不合并、不发布或创建 Release RC。
