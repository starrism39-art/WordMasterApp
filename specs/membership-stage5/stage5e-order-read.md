# Stage5-E 订单只读适配收口（2026-09-09）

范围：正常 My Orders 只读能力，仅重部署 stage5_orders。未创建平台订单、付款、发放权益、修改其他入口/权限或提交 Git。

## 修改
- membership-payment/engine.js：queryOrder 使用可信调用身份、固定订单读取；区分 UNKNOWN_ORDER / ORDER_NOT_OWNED，校验应用绑定、订单和金额快照，仅返回既有只读视图。
- membership-stage5/policy.js：提取订单记录校验；原付款路径仍保留 real() 门禁。
- membership-stage5/repository.js：固定 orders 映射和作用域的 readOrder；原写入/事务校验不放宽。
- test/membership-stage5/order-read.test.js：新增25项专项。

该能力是购买关闭或支付凭据未就绪时仍能查看本人订单的永久业务能力。不存在 testOnly、管理员绕过、客户端指定身份/集合/批次的新接口。

## 部署及回滚
环境 cloudbase-4gafzdch60ad597b；只更新 stage5_orders 代码。
备份目录 D:\membership-backups\stage5e-order-read-20260909，包含旧包、受保护配置和权限记录。
旧包 SHA256 5e144322f4bca9c0a992966995f6b3add78fae729f401d03b65d83e00debb8f9。
新包 SHA256 c2cc365ad28d8d5adb1bf06eefa8fc15bf83d5276374f9921e40c62a37132d5b。
云端 Active，修改时间 2026-09-09 21:45:27。下载回读3个源码hash与本地一致。
配置差异为空（平台掩码视图核对；本次未发送配置更新）。

## 本轮结果
本地新增25/25；必要统一回归191/191，0失败/跳过/待办。Stage2与Stage4继承，不重复运行。
真实客户端：张张张123、教师11111各9项通过，共18项。来源为当前轮实际 Console 观察汇总，未凭记忆补写请求ID。
- 各自本人测试订单返回 awaiting_payment / none。
- A查B、B查A真实存在的隔离订单均 ORDER_NOT_OWNED。
- 不存在订单分别 UNKNOWN_ORDER，没有冒充越权成功。
- 各自伪造 teacherId/openid/isAdmin/collection/env/batchId 均 UNEXPECTED_FIELDS。

仅新增2份明确TEST、未签发、无付款事实、无权益的隔离订单文档，供真实归属校验，保留审计；不是平台订单或可付款商品。购买继续关闭。
继承之前第一位老师57项、第二位老师已完成的权限/数据库/学生及双向实际权益读取证据，不重跑48项客户端矩阵。

## 尚缺及裁决
订单只读适配与双向真实归属补验 PASS。
原详细清单中的“第二位老师在参数中提交第一位老师的实际完整身份值”尚未执行；本轮已测的是伪造身份字段被拒绝。不得把二者混记。自动输入工具反复插入旧文本，未执行这些错误文本；停止重复尝试。
Stage5-E 总裁决保守为 PARTIAL，保留上述精确用例的证据缺口。当前没有发现订单归属绕过。
后续支付账号/凭据/通知/调度/实付/跨设备及业务UI均未在本轮验证，不属于本次部署结果。
下一步仅补这项真实会话参数拒绝证据并完成裁决，不重新部署或启动支付。

## 本轮只读结果核清
通过真实手机 Console 读取 s5ExactSpoof：state=stopped，results=[]，error=CACHED_TARGET_UNAVAILABLE。
此结果来自第二版缓存候选检查（openid/currentUser.id/globalData.currentUser.id）。它在目标指纹校验处停止，没有执行最终身份伪造云调用。不能计为拒绝用例PASS。
纠正之前描述：剩余项并非仅等待打印结果；需要将既有可信核验过的目标身份通过安全的测试输入方式传入当前真实会话，不能继续假设目标身份存在于手机缓存。不得新增服务端测试后门或改变白名单。既有订单18项及191项本地回归证据保持有效。
本轮无源码/部署/业务数据修改。Stage5-E仍PARTIAL。

## 2026-09-10 最后一项真实身份补验完成（取代上面的临时 PARTIAL 裁决）

设备：本轮 USB 和真机调试窗口均为 HUAWEI HLK-AL00；实际小程序会话调用 login，再在内存中将返回 AppID 和完整 OPENID 与既有可信身份资料精确比较。不是依据昵称、脱敏后缀或客户端缓存授权。
调用者指纹：763d533463b5（教师11111）。目标身份指纹：3eac6ca3b522（张张张123）。
身份资料取自既有 DPAPI 受保护配置，使用前在本地重新核对 SHA256 指纹；只在内存还原用于精确比较和请求参数，没有新增云端接口或修改配置。完整身份未明文输出或写入测试文件。

本轮只补两项真实客户端请求：
- queryOrder：orderId=stage5e_readonly_a_20260909，teacherId 参数为张张张123实际完整身份；结果 UNEXPECTED_FIELDS，PASS。
- 同一订单，openid 参数为张张张123实际完整身份；结果 UNEXPECTED_FIELDS，PASS。
控制台 s5FinalExact.state=complete，results 两项 pass=true。结果来源为实际真机 Console；SDK异常未提供两项请求ID（null），不编造请求ID。未重跑既有18/57/48项或191项本地测试。

上述拒绝来自既有正常订单接口，未新增 testOnly 绕过、管理员绕过、任意集合查询、任意老师查询或临时服务端后门。本轮无代码修改、云部署、业务数据写入、平台订单、付款、会员权益变更、Git提交或推送；仅追加验收证据。购买关闭状态继承既有配置，本轮未改变开关。

最终裁决：结合此前已保存的资源部署、客户端禁读写、可信身份、双向订单/权益归属、测试学生/免费名额、管理员拒绝及数据库事务/并发/回滚证据，Stage5-E受限测试云资源部署、真实CloudBase权限与事务联调 PASS。此次精确身份用例的证据缺口已关闭。

该结论不代表整个Stage5或真实付款PASS。OfferID/渠道/平台商品/最低金额、实际凭据与令牌管理、通知路由与调度、真实支付去重/退款/跨设备权益恢复、业务页面接入仍属后续范围。名单外真实第三账号及会员管理员正向操作没有在此次两账号补验中新增验证；不声称已验证这些场景。
下一步：单独规划并核对真实支付配置与最小联调授权；不自动启用购买、部署、配置通知/调度或付款。
